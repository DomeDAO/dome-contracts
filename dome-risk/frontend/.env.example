# Vite env vars (copy to .env.local for development).

# Required: the first block to scan when building the global withdrawal queue.
# Set this to the vault deployment block on your target chain.
VITE_QUEUE_FROM_BLOCK=0

# Optional: dedicated RPC URL for log scanning (recommended). If unset, the app uses the injected wallet provider.
VITE_RPC_URL=

# Optional: override polling interval (ms). Default is 20000.
VITE_POLL_MS=20000
